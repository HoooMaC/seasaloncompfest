import React from 'react';

const TransactionHistorySection = () => {
  // Need to get all the history section
  // const allTransactions = await getTransactionbyUserId(session.userId)

  return (
    <div className='flex flex-col gap-2'>
      {/*  Need to check if its exits or not*/}
      {/*  If it is map all of them*/}
      {/* allTransaction.map((transaction, index) ... */}
      {/*  If not, ask to make one*/}
    </div>
  );
};
export default TransactionHistorySection;
